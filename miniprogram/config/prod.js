module.exports = {
  mini: {},
  h5: {
    /* 如需分析产物体积，把下面这段打开
    webpackChain (chain) {
      chain.plugin('analyzer')
        .use(require('webpack-bundle-analyzer').BundleAnalyzerPlugin, [])
    }
    */
  }
}
